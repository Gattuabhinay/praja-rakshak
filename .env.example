# Supabase (required for real login + case history)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Admin AI fallback (server-only — used when user has no key in AI Settings)
# Preferred for hackathon demo: OpenRouter
OPENROUTER_API_KEY=

# Optional alternate admin AI fallbacks
OPENAI_API_KEY=
GEMINI_API_KEY=
